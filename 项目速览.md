# 项目速览

这份文档是给后续新会话快速建立上下文用的，不替代主 `README.md`，重点记录项目的实际结构、运行方式和当前部署形态。

## 它是什么

这是一个媒体文件自动监控、识别、刮削、重命名和归档工具。

- 后端：Python + FastAPI
- 前端：`web/dist` 下的静态 Vue 页面
- 数据库：SQLite
- 运行形态：
  - 桌面版入口：`main.py`
  - Docker / Web 入口：`docker_main.py`

项目最初带有桌面 GUI 形态，后续演进为 Web / Docker 版本。现在 Docker 中运行时，会通过 `WorkerContext` 复用原有识别与归档逻辑。

## 当前本地部署

- 仓库目录：`/home/shai102/docker/自动监控刮削源码`
- 容器名：`media-scraper`
- 镜像名：`media-scraper:latest`
- 对外端口：`8090`
- 容器启动命令：`python docker_main.py`
- 数据目录挂载：宿主 `./data` -> 容器 `/data`
- 媒体目录挂载：宿主 `/home/shai102/huahuo` -> 容器 `/media`

## 当前实际工作流

这套部署不是单段式刮削，而是两段式：

1. 第一段：`/media/strm`
   - 监控模式：`symlink_export`
   - 作用：把源目录文件按相对路径导出到 `/media/ruanstrm`

2. 第二段：`/media/ruanstrm/...刮削好的`
   - 监控模式：`rename`
   - 作用：对导出后的媒体库做识别、重命名、写入 NFO / 图片等 sidecar

当前数据库里启用的监控目录一共 5 个：

- `/media/strm`
- `/media/ruanstrm/动漫刮削好的`
- `/media/ruanstrm/动漫剧场版刮削好的`
- `/media/ruanstrm/电影刮削好的`
- `/media/ruanstrm/电视剧刮削好的`

## 启动链路

Docker 模式下的主链路如下：

1. `docker_main.py`
2. `server.py`
3. FastAPI `lifespan`
4. `init_db()`
5. `FolderWatcher(...)`
6. `FolderWatcher.start()`

关键点：

- `docker_main.py` 负责解析 `DATA_DIR`、初始化日志、启动 uvicorn
- `server.py` 负责创建 FastAPI、注册 API 路由、启动监控器、挂载前端静态资源
- `FolderWatcher` 是监控总协调器

## 文件进入后的处理链路

新文件进入后，并不是立刻处理，而是按下面的流程走：

1. `watchdog` 监听到文件事件
2. 文件进入待处理队列
3. 做 5 秒 debounce，避免文件还没写完
4. `FolderWatcher` 调用 `monitor/file_processor.py`
5. 根据监控目录配置选择模式：
   - `symlink_export`：只导出软链接，不刮削
   - `rename` / `move` / `copy` / `symlink` / `hardlink`：执行识别和归档
6. 识别完成后写 SQLite 记录、NFO、图片，并通过 WebSocket 推送前端状态

此外还有一个重要兜底：

- 每 30 秒轮询扫描一次监控目录

这样即使 Docker 环境下宿主机文件事件丢失，仍能补抓到新文件。

## 核心源码位置

最值得优先看的文件：

- `docker_main.py`：Docker 启动入口
- `server.py`：FastAPI 应用入口
- `monitor/watcher.py`：监控器总调度
- `monitor/scan_service.py`：轮询扫描、去重、入队
- `monitor/file_processor.py`：单文件主处理流程
- `monitor/file_processor_archive.py`：归档、重命名、软链接/硬链接/复制/移动
- `core/services/worker_context.py`：把旧 GUI 运行时抽象成无界面上下文
- `core/workers/task_runner.py`：识别任务主入口
- `api/routes/settings.py`：配置读写和连通性测试
- `api/routes/monitor.py`：监控目录管理
- `api/routes/records.py`：刮削记录、重试、手动匹配
- `api/routes/symlinks.py`：导出软链接记录
- `api/routes/ws.py`：实时状态推送
- `db/scrape_models.py`：核心表结构
- `db/database.py`：SQLite 初始化和轻量迁移

## 关键数据文件

运行态数据主要都在 `data/` 下：

- `data/renamer_config.json`：设置与密钥
- `data/media_renamer.db`：SQLite 主数据库
- `data/api_cache.json`：API 缓存
- `data/media_renamer.log`：应用日志
- `data/scrape_process.log`：刮削流程日志

这些文件属于本地运行数据，不应该提交到 Git。

## 当前已知设计特点

- 这是一个“旧桌面逻辑 + 新 Web 外壳”的混合项目
- 业务逻辑已经在往 `core/`、`monitor/`、`db/`、`utils/` 下拆分
- 前端现在是编译后的静态资源直接由 FastAPI 挂载
- 监控策略是“实时事件 + 周期轮询”双轨制
- 支持删除同步、元数据巡检、Telegram 通知、Emby/Jellyfin 通知

## 下次接手时的建议阅读顺序

如果下次只是要理解项目，推荐按这个顺序看：

1. 先看本文件
2. 再看 `server.py`
3. 再看 `monitor/watcher.py`
4. 再看 `monitor/file_processor.py`
5. 最后根据问题进入 `api/routes/*` 或 `core/*`

如果下次是排查“为什么没有自动处理新文件”，优先看：

1. `docker logs media-scraper`
2. `data/scrape_process.log`
3. `monitor/watcher.py`
4. `monitor/scan_service.py`

如果下次是排查“为什么识别错了 / 没写 NFO / 没重命名”，优先看：

1. `monitor/file_processor.py`
2. `core/workers/task_runner.py`
3. `monitor/file_processor_archive.py`
4. `core/records/manual_service.py`

## Git 说明

为了避免把本地运行数据和密钥推上去，仓库现在应忽略：

- `data/`
- `.env`
- SQLite 临时文件
- 本地 compose override

如果后续新增本地运行态文件，默认也应该优先放进 `data/`。
